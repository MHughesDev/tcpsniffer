{
  "name": "tcp-sniffer-injector",
  "version": "0.1.0",
  "type": "module",
  "description": "Mutating admission webhook that injects the TCP Sniffer sidecar into pods (Stream C)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test dist/",
    "test:ts": "tsc && node --test dist/"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
