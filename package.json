{
  "name": "tcp-sniffer",
  "version": "0.1.0",
  "type": "module",
  "description": "TCP Sniffer — capture-only library with C++ engine; TS API per docs/OVERVIEW.md",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md", "LICENSE"],
  "repository": { "type": "git", "url": "https://github.com/your-org/tcpsniffer.git" },
  "homepage": "https://github.com/your-org/tcpsniffer#readme",
  "bugs": { "url": "https://github.com/your-org/tcpsniffer/issues" },
  "scripts": {
    "build": "tsc",
    "build:native": "node-gyp configure build",
    "rebuild": "node-gyp rebuild",
    "start": "node dist/entrypoint.js",
    "demo": "node dist/demo.js",
    "test": "node --test dist/",
    "test:ts": "tsc && node --test dist/"
  },
  "keywords": ["tcp", "sniffer", "libpcap", "http", "kubernetes", "sidecar"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "node-addon-api": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
